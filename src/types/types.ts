export type Category="general"|"work"|"home"|"hobby";

/*interface Task {
    title: string;
    done: boolean;
    category?: Category;
};*/
/*enum Category {
    GENERAL="general",
    WORK="work",
    HOME="home",
    HOBBY="hobby"
};*/